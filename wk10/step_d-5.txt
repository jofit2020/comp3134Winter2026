Step D-5 Observations:

Using q=%2e%2e%2f (encoded ../) still allows directory traversal.

Issue:
The application does not handle encoded inputs, so attackers can bypass simple filters using URL encoding.

Sensitive Information:
- Directory structure outside the web root
- Internal system layout
- Hidden files and folders
- Information useful for further exploitation